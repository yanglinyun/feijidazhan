package {

    public class Guider extends StaticGameItem {
        
        protected var that = this;
        public function Guider(posX:Number, posY:Number, width:Number = 0, height:Number = 0, rotation:Number = 0) {
            // constructor code
            super(posX, posY, width, height, rotation);
        }

    }

}
