package  {
	
	public class Move {
		protected var direction:Array;// 8个方向
		protected var frameSpeed:Number;
		protected var moveNode:Array;
 		public function Move() {
			this.direction = direction;
			this.frameSpeed = frameSpeed;
			this.moveNode = moveNode;
		}

	}
	
}
